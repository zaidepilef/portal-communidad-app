import packageInfo from '../../package.json';

export const environment = {

	//apiUrl: 'http://localhost:8080/api',
	apiUrl: 'https://ougt.cl/project-root/public/api',
	appVersion: packageInfo.version,
	production: false
};
